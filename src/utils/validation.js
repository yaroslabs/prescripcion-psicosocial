// Ningún dato del formulario es obligatorio para generar la matriz: la
// función se conserva (en vez de eliminar su uso en App.jsx) para no tocar
// más de lo necesario, pero ya no bloquea la generación.
export function validarDatos() {
  return [];
}
